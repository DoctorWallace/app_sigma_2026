# Quality Templates

This directory stores versioned quality templates per lab and revision.
Each lab has its own folder, with subfolders like `REV001`, `REV002`, etc.
Mapping files must list headers/placeholders and the source model fields so
exports can be validated by tests without relying on UI changes.
